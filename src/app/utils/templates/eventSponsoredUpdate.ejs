<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Sponsorship Confirmation - LinkUp</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family:
          -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          'Helvetica Neue', Arial, sans-serif;
        background-color: #ffffff;
        -webkit-font-smoothing: antialiased;
      }
      .email-container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
      }
      .header {
        padding: 40px 40px 32px 40px;
        border-bottom: 1px solid #e5e5e5;
      }
      .logo {
        color: #000000;
        font-size: 24px;
        font-weight: 600;
        margin: 0;
        letter-spacing: -0.3px;
      }
      .content {
        padding: 40px;
      }
      .status {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background-color: #000000;
        color: #ffffff;
        padding: 8px 16px;
        border-radius: 4px;
        font-size: 14px;
        font-weight: 500;
        margin-bottom: 32px;
      }
      .status-icon {
        font-size: 16px;
      }
      h1 {
        color: #000000;
        font-size: 24px;
        font-weight: 600;
        margin: 0 0 24px 0;
        line-height: 1.3;
      }
      p {
        color: #525252;
        font-size: 16px;
        line-height: 1.6;
        margin: 0 0 16px 0;
      }
      .event-details {
        margin: 40px 0;
        padding: 32px;
        background-color: #fafafa;
        border-radius: 8px;
      }
      .detail-row {
        display: flex;
        padding: 16px 0;
        border-bottom: 1px solid #e5e5e5;
      }
      .detail-row:first-child {
        padding-top: 0;
      }
      .detail-row:last-child {
        border-bottom: none;
        padding-bottom: 0;
      }
      .detail-label {
        font-size: 14px;
        color: #737373;
        min-width: 140px;
        font-weight: 500;
      }
      .detail-value {
        font-size: 16px;
        color: #000000;
        font-weight: 500;
        flex: 1;
      }
      .cta-button {
        display: inline-block;
        background-color: #000000;
        color: #ffffff;
        text-decoration: none;
        padding: 14px 28px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 15px;
        margin: 32px 0;
      }
      .signature {
        margin-top: 40px;
        padding-top: 24px;
        border-top: 1px solid #e5e5e5;
      }
      .signature p {
        margin: 0;
        color: #000000;
        font-size: 15px;
      }
      .signature-team {
        font-weight: 600;
      }
      .footer {
        padding: 32px 40px;
        background-color: #fafafa;
        border-top: 1px solid #e5e5e5;
      }
      .footer p {
        color: #737373;
        font-size: 14px;
        margin: 0 0 8px 0;
        text-align: center;
      }
      .footer a {
        color: #000000;
        text-decoration: none;
      }
      .footer-links {
        text-align: center;
        margin-top: 16px;
        padding-top: 16px;
        border-top: 1px solid #e5e5e5;
      }
      .footer-links a {
        color: #737373;
        font-size: 13px;
        margin: 0 8px;
      }
      @media only screen and (max-width: 600px) {
        .header,
        .content,
        .footer {
          padding-left: 24px;
          padding-right: 24px;
        }
        .event-details {
          padding: 24px;
        }
        .detail-row {
          flex-direction: column;
          gap: 4px;
        }
        .detail-label {
          min-width: auto;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <!-- Header -->
      <div class="header">
        <h2 class="logo">LinkUp</h2>
      </div>

      <!-- Main Content -->
      <div class="content">
        <div class="status">
          <span class="status-icon">✓</span>
          <span>Sponsored</span>
        </div>

        <h1>Your event has been successfully sponsored</h1>

        <p>Dear Event Host,</p>

        <p>
          Your event <strong><%= event_title %></strong> is now sponsored on
          LinkUp. This sponsorship provides enhanced visibility and promotional
          support to help you reach more attendees.
        </p>

        <!-- Event Details -->
        <div class="event-details">
          <div class="detail-row">
            <div class="detail-label">Event</div>
            <div class="detail-value"><%= event_title %></div>
          </div>
          <div class="detail-row">
            <div class="detail-label">Venue</div>
            <div class="detail-value"><%= event_venue %></div>
          </div>
          <div class="detail-row">
            <div class="detail-label">Date</div>
            <div class="detail-value"><%= event_date %></div>
          </div>
          <div class="detail-row">
            <div class="detail-label">Amount</div>
            <div class="detail-value">$<%= amount %> <%= currency %></div>
          </div>
          <div class="detail-row">
            <div class="detail-label">Payment Date</div>
            <div class="detail-value"><%= date %></div>
          </div>
        </div>

        <p>
          We recommend reviewing your event details to ensure all information is
          current and complete to maximize engagement.
        </p>

        <!-- <a href="myshoppingapp://product/<%= eventId %>" class="cta-button">
          View Event
        </a> -->

        <!-- Signature -->
        <div class="signature">
          <p>Best regards,</p>
          <p class="signature-team">The LinkUp Team</p>
        </div>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>
          Need help? Contact us at
          <a href="mailto:<%= support_email %>"><%= support_email %></a>
        </p>
        <div class="footer-links">
          <a href="#">Privacy</a>
          <a href="#">Terms</a>
          <a href="#">Unsubscribe</a>
        </div>
        <p style="margin-top: 16px; font-size: 13px">
          © 2024 LinkUp. All rights reserved.
        </p>
      </div>
    </div>
  </body>
</html>
